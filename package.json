{
  "name": "nvro-discord-bot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^5.6.1",
    "typescript": "^4.8.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "test": "lerna run --parallel test"
  }
}
